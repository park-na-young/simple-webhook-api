server:
  port : 9090

spring:
  datasource:
      url: jdbc:mysql://localhost:3306/webhook_test?serverTimezone=Asia/Seoul
      username: root
      password: 1234
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
      generate-ddl: true
      open-in-view: false
  hibernate:
    # ex) ddl-auto: none, create-drop, create, update, validate
    ddl-auto : update
    show_sql : true
    format_sql : true
    use_sql_comments : true
